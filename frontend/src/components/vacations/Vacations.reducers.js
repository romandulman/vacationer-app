// in reducer.js
import {VacConstants} from './Vacations.constants'

const initialState = {
    Data: 'Royksopp'
};

export default (state=initialState, action={}) => {
    switch (action.type) {
        case types.SEARCH:
            return [
alert()
            ];
        // ...
    }
};
