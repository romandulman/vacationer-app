// in reducer.js
import * as types from './Vacations.types';

const initialState = {
    text: 'Royksopp'
};

export default (state=initialState, action={}) => {
    switch (action.type) {
        case types.SEARCH:
            return [
alert()
            ];
        // ...
    }
};
